{% extends 'base.html.twig' %}

{% block title %}Licencie index{% endblock %}

{% block body %}
    <h1>Licencie index</h1>

<div class="body-wrapper">
 <div class="container-fluid">
   <div class="card bg-info-subtle shadow-none position-relative overflow-hidden mb-4">
     <div class="card-body px-4 py-3">
      <div class="row align-items-center">
        <div class="col-9">
          <h4 class="fw-semibold mb-8">Licencie</h4>
          <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
              <li class="breadcrumb-item">
                <a class="text-muted text-decoration-none" href="https://bootstrapdemos.adminmart.com/modernize/dist/main/index.html"
                  >Home</a
                >
            </li>
            <li class="breadcrumb-item" aria-current="page">Licencie</li>
          </ol>
        </nav>
      </div>
      <div class="col-3">
        <div class="text-center mb-n5">
          <img
            src="https://bootstrapdemos.adminmart.com/modernize/dist/assets/images/breadcrumb/ChatBc.png"
            alt=""
            class="img-fluid mb-n4"
          />
        </div>
      </div>
    </div>
  </div>
</div>

          <div class="widget-content searchable-container list">
            <div class="card card-body">
              <div class="row">
                
      <form>
        <div class="form-group">
            <label for="categorieLabel">Catégorie</label>
            <select class="form-control" id="categorieLabel" name="categorie">
              {#  {% for categorie in categories %}
                    <option value="{{ categorie.id }}">{{ categorie.nom_categorie }}</option>
                {% endfor %}
               #} 
            </select>
           
        </div>
        </form>
       
                <div
                  class="col-md-8 col-xl-9 text-end d-flex justify-content-md-end justify-content-center mt-3 mt-md-0">
                  
                  <a href="{{ path('app_licencie_new') }}"  class="btn btn-info d-flex align-items-center">
                    Create new
                  </a>
                </div>
             
            </div>
            {# <div #}
            
            <div class="card card-body">
              <div class="table-responsive">

     <table class="table search-table align-middle text-nowrap">
                  
        <thead class="header-item">
            <tr>
                <th>Id</th>
                <th>NumLicence</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Catégorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for licencie in licencies %}
            <tr class="search-items">
           <td> <span class="usr-id" >{{ licencie.id }}</span></td>
           <td> <span class="usr-id" >{{ licencie.numLicence }}</span> </td>
           <td> <span class="usr-name" >{{ licencie.nom }}</span> </td>
           <td> <span class="usr-name" >{{ licencie.prenom }}</span> </td>
          <td> <span class="usr-info" > {{ licencie.categorie.nom }}</span> </td>
              
                <td>
                    <a href="{{ path('app_licencie_show', {'id': licencie.id}) }}">show</a>
                    <a href="{{ path('app_licencie_edit', {'id': licencie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
   
    
{% endblock %}



  
     


