{% extends 'base.html.twig' %}

{% block title %}Détails du mail{% endblock %}

{% block body %}
    <div class="card w-100">
        <div class="card-body p-4">
            <div class="d-flex justify-content-between">
                <h5 class="card-title fw-semibold mb-4">Détails du mail</h5>
            </div>
            <!-- Flash message section -->
            {# {% include 'components/flashMsg.html.twig' %} #}
            <!-- Form -->
            <div class="col-md-12">
                <div class="card">
                    <div class="card-body">
                        {{ include('mail_edu/_form.html.twig', {'disable_btn': true}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
