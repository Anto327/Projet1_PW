{% extends 'base.html.twig' %}

{% block title %}New Categorie{% endblock %}

{% block body %}
    <h1>Create new Categorie</h1>
     <div class="col-lg-6">  
          <div class="card-body p-4">
            {{ include('categorie/_form.html.twig', {'form': form}) }}
           </div>
     </div>  
     <div class="mb-4">
        <a href="{{ path('app_categorie_index') }}" class="btn btn-success">back to list</a>
        </div>

{% endblock %}