{% extends 'base.html.twig' %}

{% block title %}Educateur index{% endblock %}

{% block body %}
    <h1>Educateur index</h1>

    <
     <div class="body-wrapper">
        <div class="container-fluid">
          <div class="card bg-info-subtle shadow-none position-relative overflow-hidden mb-4">
  <div class="card-body px-4 py-3">
    <div class="row align-items-center">
      <div class="col-9">
        <h4 class="fw-semibold mb-8">Educateur</h4>
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item">
              <a class="text-muted text-decoration-none" href="https://bootstrapdemos.adminmart.com/modernize/dist/main/index.html"
                >Home</a
              >
            </li>
            <li class="breadcrumb-item" aria-current="page">Educateur</li>
          </ol>
        </nav>
      </div>
      <div class="col-3">
        <div class="text-center mb-n5">
          <img
            src="https://bootstrapdemos.adminmart.com/modernize/dist/assets/images/breadcrumb/ChatBc.png"
            alt=""
            class="img-fluid mb-n4"
          />
        </div>
      </div>
    </div>
  </div>
</div>

          <div class="widget-content searchable-container list">
            <div class="card card-body">
              <div class="row">
                
      <form>
        <div class="form-group">
            <label for="categorieLabel">Catégorie</label>
            <select class="form-control" id="categorieLabel" name="categorie">
              {#  {% for categorie in categories %}
                    <option value="{{ categorie.id }}">{{ categorie.nom_categorie }}</option>
                {% endfor %}
               #} 
            </select>
           
        </div>
        </form>
       
                <div
                  class="col-md-8 col-xl-9 text-end d-flex justify-content-md-end justify-content-center mt-3 mt-md-0">
                  <a href="{{ path('app_educateur_new') }}" class="btn btn-info d-flex align-items-center">Create new</a>
                  
                </div>
             
            </div>
            {# <div #}
            
            <div class="card card-body">
              <div class="table-responsive">
                <table class="table search-table align-middle text-nowrap">
                  <thead class="header-item">
                    
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Password</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for educateur in educateurs %}
            <tr>
                <td>{{ educateur.id }}</td>
                <td>{{ educateur.email }}</td>
                <td>{{ educateur.roles ? educateur.roles|json_encode : '' }}</td>
                <td>{{ educateur.password }}</td>
                <td>
                    <a href="{{ path('app_educateur_show', {'id': educateur.id}) }}">show</a>
                    <a href="{{ path('app_educateur_edit', {'id': educateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}


